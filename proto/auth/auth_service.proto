syntax = "proto3";

package auth;

option go_package = "pkg/auth";


service AuthService{
    rpc AuthenticateUser(RequestType) returns (ResponseType);
}

message RequestType {
    string user_name = 1;
    string user_passwd = 2;
}

message ResponseType {
    bool response = 1;
}

